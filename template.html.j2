<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>{{ data['site']['title'] }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="robots" content="nofollow, noarchive">
  <meta name="description" content="{{ data['site']['description'] }}">
  <meta name="keywords" content="{{ data['site']['keywords']|join(', ') }}">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.1.3/css/bootstrap.min.css" integrity="sha512-GQGU0fMMi238uA+a/bdWJfpUGKUkBdgfFdgBm72SUQ6BeyWjoY/ton0tEjH+OSH9iP4Dfh+7HM0I9f5eR0L/4w==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    a {
      text-decoration: none;
      color: #2361a1;
    }
    tr td {
      padding-left: 0 !important;
    }
    tr th {
      padding-left: 0 !important;
    }
    td a {
      color: #000;
    }
    strong em {
      color: #ff8a00;
      font-size: 0.6em;
    }
  </style>
</head>

<body class="py-3">
  <div class="container">
  <div class="row pt-3 mt-5 mb-3">
    <div class="col-md mx-3">
      <h1>{{ data['site']['title']|upper }}</h1>
    </div>
  </div>
  {% for group in data['feeds'] %}
  <div class="row py-3">
    {% for feed in group %}
    <div class="col-md mx-3">
      <table class="table">
        <thead>
          <tr>
            <th scope="col"><h4><a href="{{ feed['front_page'] }}" target="_blank">{{ feed['name'] }}</a></h4></th>
          </tr>
        </thead>
        <tbody>
          {% for post in feed['posts'] %}
          <tr>
            <td><a href="{{ post['link'] }}" target="_blank">{{ post['title'] }}</a>{% if post['new'] == true %}<strong><em>&nbsp;&nbsp;&nbsp;NEW</em></strong>{% endif %}</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
    {% endfor %}
  </div>
  {% endfor %}
  <div class="row py-2"></div>
  {% for group in data['extra_links'] %}
  <div class="row p-0">
    {% for link in group %}
    <div class="col-md mx-3">
      <h6><a href="{{ link['link'] }}" target="_blank">{{ link['name'] }}</a></h6>
    </div>
    {% endfor %}
  </div>
  {% endfor %}
</div>
</body>

</html>
